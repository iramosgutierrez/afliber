/*
$(document).ready(function(){

    function resizeCarousel() {
        if($(window).width() < 768) {
            $('#catalogueCarousel')
              .removeClass('w-50')
              .addClass('w-100');
        } else {
            $('#catalogueCarousel')
              .removeClass('w-100')
              .addClass('w-50');
        }
    }
    resizeCarousel();

    $(window).resize(resizeCarousel);

});
*/
